<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent"
    android:background="@color/background"
    android:gravity="center"
    android:orientation="vertical"
    android:padding="35dip" >

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:layout_marginBottom="20dip"
        android:text="@string/Letris"
        android:textSize="24.5sp" />

    <TableLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:stretchColumns="*" >

        <TableRow>

            <Button
                android:id="@+id/letris_continue_button"
                android:text="@string/continue_label" />

            <Button
                android:id="@+id/letris_new_button"
                android:text="@string/new_game_label" />

            <Button
                android:id="@+id/letris_instruction"
                android:text="Instruction" />
        </TableRow>

        <TableRow>

            <Button
                android:id="@+id/letris_about_button"
                android:text="@string/about_label" />

            <Button
                android:id="@+id/letris_acknowledge_button"
                android:text="@string/acknowledge" />

            <Button
                android:id="@+id/letris_exit_button"
                android:text="@string/exit_label" />
        </TableRow>
    </TableLayout>

</LinearLayout>